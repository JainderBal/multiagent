{
  "name": "multiagent",
  "version": "0.0.0",
  "type": "module",
  "bin": { "multiagent": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "dev": "vitest"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "commander": "^12.1.0",
    "zod": "^3.23.8",
    "execa": "^9.3.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "@types/node": "^20.14.0"
  }
}
